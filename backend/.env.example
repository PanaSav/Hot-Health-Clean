# Backend environment (copy to .env and fill in your values)

PORT=4000
PUBLIC_BASE_URL=http://10.0.0.125:4000
ADMIN_PASSWORD=Hotest
# Leave blank to auto-generate; app will persist to .env on first run
ENCRYPTION_KEY=
# 0 = delete raw audio after processing, >0 to retain N days (raw uploads folder)
RETENTION_DAYS=0
# Text model for parsing & translation
OPENAI_TEXT_MODEL=gpt-4o
